//Vamos concatenar os arrays transformar os dois em um só
// Usando concat();

const a1 = [1,2,3];
const a2 = [4,5,6];
//Além de concatenar, adicione o que voce quiser na lista
const a3 = a1.concat(a2, [7,8,9], 'Mais um elem'); 
console.log(a3);
